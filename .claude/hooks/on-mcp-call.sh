#!/bin/sh
# on-mcp-call.sh
# Hook acionado quando uma ferramenta MCP é chamada (PreToolUse ou PostToolUse).
# Use para logar chamadas, validar contexto ou reagir a ferramentas específicas.
# Configurar em .claude/settings.json no bloco "hooks".

